# FLUX endpoint on CentML

## Quickstart

1. follow instructions in centml-endpoint directory to deploy the service. 

2. use curl or the provided example apps in gradio and requests directory to generate images. 